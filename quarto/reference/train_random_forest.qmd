# train_random_forest { #data_science.nodes.train_random_forest }

`nodes.train_random_forest(X_train, y_train)`

Trains the Random Forest model.

Args:
    X_train: Training data of independent features.
    y_train: Training data for target decision.

Returns:
    Trained model.